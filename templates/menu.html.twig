<header class="bg-light sticky-top">
    <nav class="navbar container navbar-expand navbar-light ">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="{{ path("app_home") }}" class="nav-link"><img src="/upload/shop.png" alt="Logo"></a>
                
            </li>
        </ul>

        <ul class="navbar-nav ms-auto">
        {% if not app.user %}
            <li>
                <a href="{{ path("app_login") }}" class="nav-link">Connexion</a>
            </li>
            <li>
                <a href="{{ path("app_inscription") }}" class="nav-link">Inscription</a>
            </li>
            {% else %}
            <li>
                <a href="{{ path("app_logout") }}" class="nav-link">Déconnexion</a>
            </li>
            <li>
                <a href="{{ path("compte") }}" class="nav-link">Mon compte</a>
            </li>
            {% endif %}
            <li>
                <a href="{{ path("panier") }}" class="nav-link">Panier <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="#949494" class="bi bi-bag-fill" viewBox="0 0 16 16">
                <path d="M8 1a2.5 2.5 0 0 1 2.5 2.5V4h-5v-.5A2.5 2.5 0 0 1 8 1zm3.5 3v-.5a3.5 3.5 0 1 0-7 0V4H1v10a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V4h-3.5z"/>
                </svg></a>
            </li>
            <li>
                <a href="{{ path("contact") }}" class="nav-link">Contact</a>
            </li>
                
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a href="{{ path("app_commande_index") }}" class="nav-link">| Back Office</a>
            </li>
            {% endif %}
        </ul>
        
    </nav>
</header>
